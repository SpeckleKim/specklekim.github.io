---
layout: default
title: 토크나이저 설계와 어휘
lang: ko
---

# 토크나이저 설계와 어휘

[토크나이제이션](/tokenization-kor.html)은 언어 모델 처리의 중대한 첫 단계로, 원본 텍스트를 이산적인 토큰 시퀀스로 변환합니다. [토크나이저](/tokenization-kor.html)의 설계는 모델 효율성, 다국어 능력, 일반화 성능에 크게 영향을 미칩니다.

## 어휘 크기와 모델 성능

어휘 크기(고유 토큰의 수)는 [토크나이저](/tokenization-kor.html) 설계의 근본적인 트레이드오프를 나타냅니다:

**작은 어휘 (10K-30K 토큰)**:
- 더 적은 고유 토큰이 모델 매개변수를 줄임
- 같은 텍스트에 대해 더 긴 시퀀스 (더 많은 계산 필요)
- 어휘 밖의 단어에 대한 더 나은 일반화
- 형태 변화가 풍부한 언어에 적합

**큰 어휘 (50K-250K 토큰)**:
- 같은 텍스트에 대해 더 짧은 시퀀스 (더 적은 계산)
- 공통 부분단어의 더 나은 압축
- 더 많은 모델 용량이 어휘 임베딩에 할당
- 흡수 언어(agglutinative language)와 다국어 설정에 적합

**최적 범위**: 대부분의 현대적 LLM의 경우 30K-50K 토큰이 실질적인 균형을 나타냅니다. [GPT](/gpt-kor.html)-2는 50K를 사용했고, LLAMA는 32K를, Claude는 100K 이상의 토큰을 사용합니다.

관계는 단순히 더 많은 어휘가 더 나은 성능과 같지 않습니다:
- 매우 큰 어휘는 드문 토큰에 모델 용량을 낭비
- 매우 작은 어휘는 시퀀스 길이를 증가시켜 효율성을 해침
- 최적 어휘는 언어 범위와 작업 요구사항에 따라 달라짐

## BPE 학습: 바이트-쌍 인코딩

바이트-쌍 인코딩(Byte-Pair Encoding, BPE)은 현대적 언어 모델에 사용되는 지배적인 [토크나이제이션](/tokenization-kor.html) 알고리즘입니다:

**알고리즘**:
1. 모든 개별 바이트(UTF-8의 경우 256개 토큰)로 시작
2. 코퍼스에서 인접 바이트 쌍의 빈도 계산
3. 가장 빈번한 쌍을 새로운 토큰으로 병합
4. 원하는 어휘 크기에 도달할 때까지 반복

**예시**:
```
"data"를 바이트로 표현: d a t a
빈번: "da" → 새로운 토큰으로 병합 <da>
결과: <da> t a
```

**장점**:
- 모든 UTF-8 텍스트(알려지지 않은 문자 포함)를 처리
- 데이터에서 의미 있는 부분단어 단위 학습
- 결정론적이고 재현 가능
- 많은 언어에서 잘 작동

**학습 데이터의 중요성**: BPE [토크나이저](/tokenization-kor.html) 품질은 학습 코퍼스 다양성에 크게 의존합니다. 특정 언어를 충분히 나타내지 않는 코퍼스는 그 언어에 대한 비효율적인 [토크나이제이션](/tokenization-kor.html)을 초래합니다.

## 다국어 토크나이제이션의 도전과제

다국어를 위한 단일 [토크나이저](/tokenization-kor.html) 생성은 복잡성을 도입합니다:

**문자 분포 차이**: 언어는 서로 다른 문자 빈도와 알파벳을 가집니다. 영어에 최적화된 [토크나이저](/tokenization-kor.html)는 중국어나 아랍어를 비효율적으로 토크나이즈할 수 있습니다.

**문자 체계별 이슈**:
- 라틴 알파벳을 사용하는 언어(영어, 스페인어, 독일어)는 BPE로 잘 압축됨
- 고유 문자 체계를 가진 언어(아랍어, 데바나가리, CJK)는 문자당 더 많은 바이트 필요
- 혼합 문자 체계 텍스트는 문자 체계 경계의 신중한 처리 필요

**언어별 압축**: 이상적으로 각 언어는 자체 [토크나이저](/tokenization-kor.html)를 가져야 하지만 실질적 시스템은 다국어 토크나이저를 사용합니다. 이는 일부 언어가 더 나은 압축을 받으면서 다른 언어는 같은 정보에 더 많은 토큰을 사용한다는 것을 의미합니다.

**해결 전략**:
- BPE 학습 중 충분히 나타나지 않은 언어 오버샘플링
- 중요한 언어 계통에 어휘 용량 전담
- 가능한 경우 언어별 별도 [토크나이저](/tokenization-kor.html) 사용
- 언어별 [토크나이제이션](/tokenization-kor.html) 효율성 메트릭 모니터링

## 비옥도: 단어당 토큰

토큰 비옥도(평균적으로 한 단어를 나타내는 토큰 수)는 주요 메트릭입니다:

- **낮은 비옥도 (1.0-1.3)** 는 효율적인 인코딩을 의미; 압축에 유용
- **높은 비옥도 (2.0+)** 는 비효율적인 인코딩을 의미; 더 많은 토큰 필요
- 언어 차이: 영어는 일반적으로 1.1-1.2, 일본어는 1.8-2.0

높은 비옥도 언어는 다음이 필요합니다:
- 같은 정보에 대해 더 많은 토큰이 [컨텍스트 윈도우](/context-window-kor.html)에 필요
- 처리 중 더 많은 계산
- 효과적인 컨텍스트 길이 감소

다국어 모델의 경우, 언어 전체의 불균등한 비옥도는 모델 성능에 편향을 일으킬 수 있습니다. 모델은 더 적은 컨텍스트가 필요하기 때문에 낮은 비옥도를 가진 언어에서 더 나은 성능을 보일 수 있습니다.

## 토큰 치유 및 패딩

**토큰 치유**는 자동 회귀 생성에서 문제를 해결합니다: 모델 출력이 잘리고 재시작될 때 경계의 [토크나이제이션](/tokenization-kor.html)이 완전한 텍스트의 최적 토크나이제이션과 일치하지 않을 수 있습니다.

예시:
```
원본: "Unexpected" → [Un, expected]
잘린: "Unex" → [Un, ex] (다른 토크나이제이션!)
```

**해결책**:
- 경계 정보를 저장하고 컨텍스트를 재토크나이즈
- 인코딩 전환을 신호하기 위해 특수 경계 토큰 사용
- 생성 재개 시 컨텍스트에서 겹침 구현

**패딩 토큰**: 시퀀스를 같은 길이로 채우는 데 사용되는 특수 토큰:
- 가변 길이 시퀀스의 배치 처리 활성화
- 학습 중 마스킹이 아닌 학습(더 나은 결과)되어야 함
- 신중하게 처리하지 않으면 모델 동작에 영향을 미칠 수 있음

## 특수 토큰과 그 역할

현대적 [토크나이저](/tokenization-kor.html)는 일반 어휘 이상의 특수 토큰을 포함합니다:

**일반적 특수 토큰**:
- `<|begin_of_text|>`: 시퀀스 시작
- `<|end_of_text|>`: 시퀀스 끝
- `<|padding|>`: 패딩 토큰
- `<|unk|>`: 알 수 없는 토큰
- `<|im_start|>`, `<|im_end|>`: 대화 마커
- `<tool_use>`: [에이전트](/agent-kor.html) 시스템을 위한 도구 호출 마커

**모델에 미치는 영향**:
- 너무 많은 특수 토큰이 어휘 용량을 낭비
- 잘 선택된 특수 토큰은 작업 특정 성능을 향상시킴
- 다양한 특수 토큰 규칙이 모델 상호운용성을 제한

**최신 접근법**: 일부 모델은 더 나은 일반화를 위해 명시적 특수 토큰에서 벗어나 특정 문자열이나 숫자 패턴을 대신 사용합니다.

## 토크나이저 평가 메트릭

여러 메트릭이 [토크나이저](/tokenization-kor.html) 품질을 평가합니다:

**압축 비율**:
```
압축 = 문자 / 토큰
```
더 높을수록 더 좋음; 일반적인 값은 영어의 경우 3-4입니다.

**어휘 효율성**:
```
효율성 = 단어당 평균 토큰
```
더 낮을수록 더 효율적; 일반적: 영어의 경우 1.1-1.3.

**언어 범위**: 알 수 없는 토큰이 필요한 테스트 코퍼스의 백분율(<1%는 좋음).

**교차언어 일관성**: 다양한 언어의 유사 단어가 유사하게 토크나이즈되는지 여부.

**하류 성능**: 궁극적으로, 보유된 테스트 세트의 하류 작업 성능이 가장 중요한 메트릭입니다.

## 모델 아키텍처에 미치는 영향

[토크나이저](/tokenization-kor.html) 설계 선택은 전체 시스템을 통해 전파됩니다:

1. **임베딩 계층 크기**: 어휘 크기가 직접 임베딩 매개변수 결정
2. **소프트맥스 출력 크기**: 다음 토큰 예측은 어휘 크기에 대한 소프트맥스 필요
3. **컨텍스트 길이**: 더 큰 어휘는 짧은 토큰을 의미하여 같은 토큰 수로 더 긴 [컨텍스트 윈도우](/context-window-kor.html) 허용
4. **시퀀스 길이 분포**: 나쁜 [토크나이제이션](/tokenization-kor.html)이 더 긴 시퀀스를 생성하여 메모리 및 계산에 영향

이러한 아키텍처 영향은 [토크나이저](/tokenization-kor.html) 선택이 학습 속도에서 추론 지연 시간까지 모든 것에 영향을 미친다는 것을 의미합니다.

## 실질적 고려사항

**기존 토크나이저 사용**: 대부분의 현대적 애플리케이션은 커스텀 [토크나이저](/tokenization-kor.html)를 학습하기보다는 게시된 토크나이저([GPT](/gpt-kor.html), LLAMA, Claude)를 사용합니다. 이는 기존 모델과의 호환성을 제공합니다.

**토크나이저 미세조정**: 전문 어휘(도메인 특정 용어, 새로운 언어)를 추가할 때, 대부분의 팀은 처음부터 재학습하기보다는 기존 [토크나이저](/tokenization-kor.html)를 확장합니다.

**토크나이제이션 일관성**: 재현 가능성과 모델 이전에 중요. 모든 [토크나이제이션](/tokenization-kor.html) 변경은 성능 변화를 유발할 수 있습니다.

**토크나이제이션 디버깅**: 일반적인 문제는 구두점, 숫자, 특수 문자의 처리를 포함합니다. [토크나이저](/tokenization-kor.html) 시각화 도구를 사용하여 모델의 텍스트 보기를 이해하세요.

[토크나이제이션](/tokenization-kor.html)은 종종 간과되지만 모델 효율성과 성능에 미치는 영향은 깊습니다. 잘 설계된 [토크나이저](/tokenization-kor.html)는 모델이 텍스트를 더 효율적으로 처리하고 도메인 및 언어에 더 잘 일반화할 수 있게 합니다.
